#include <iostream>
#include <string>
#include <fstream>

double calculate(std::string filename) {

    std:: ifstream { filename };
    if (input) {//if open? can we read? is it not empty?
        std::string name{}, catagory{};
        char gender{};
        int age{};
        // while (input) {// as long as I can read from input
    }
    while(input<<input >> word >> age >> gender >> catagory) {
        std::cout << name << " " << catagory << '\n'
    }